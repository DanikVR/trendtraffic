# Папки зависимостей
node_modules/
jspm_packages/

# Директории сборки
dist/
build/
.next/
out/
.tsbuildinfo
*.tsbuildinfo

# Логи отладки
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Файлы окружения (конфиденциально)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Системные файлы и IDE
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db
ehthumbs.db
Desktop.ini

# Локальные данные Docker (БД)
/pgdata/

# Секреты и runtime-данные (НЕ коммитить!)
# google-oauth.json — учётные данные Google OAuth
# system-config.json — рантайм-конфиг с секретами
# db_fallback.json — локальная JSON-БД с PII (email пользователей)
# superadmin-auth.json — bcrypt-хэш пароля супер-админа (изменяемый пароль)
**/google-oauth.json
**/system-config.json
**/db_fallback.json
**/db_fallback.test.json
**/db_fallback.json.bak
**/db_fallback.json.corrupt-*
**/.db_fallback*.tmp-*
**/superadmin-auth.json
*.env
!.env.example
!.env.test
# Загруженные пользователями медиа (хранятся на диске, не в git)
**/uploads/

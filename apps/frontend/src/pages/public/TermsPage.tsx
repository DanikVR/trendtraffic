/** TermsPage — Условия использования TrendTraffic (тексты: locales/<lng>/legal.json). */

import { LegalDocPage } from './LegalDocPage';

export function TermsPage() {
  return <LegalDocPage doc="terms" />;
}

export default TermsPage;

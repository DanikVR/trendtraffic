# Рендер-воркер TrendTraffic — ставится в venv OpenMontage (/opt/openmontage/.venv),
# чтобы инструменты OpenMontage импортировались в том же интерпретаторе.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
